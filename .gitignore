# ============================================
# HirePilot AI — .gitignore
# Anything listed here is never uploaded to GitHub.
# ============================================

# --- SECRETS (most important) ---
# Never commit API keys. If one leaks into a public repo,
# bots find it within minutes and run up charges on your account.
.env
.env.local
.env.*.local
secrets.json
config.local.js

# --- DEPENDENCIES ---
node_modules/
package-lock.json
yarn.lock

# --- BUILD OUTPUT ---
dist/
build/
.cache/

# --- SUPABASE CLI LOCAL STATE ---
# Machine-specific cache from `supabase link`/`functions deploy` —
# not secrets, but local-only and never meant to be shared.
supabase/.temp/

# --- EDITOR SETTINGS ---
.vscode/
.idea/
*.swp
*.swo

# --- OPERATING SYSTEM JUNK ---
.DS_Store
Thumbs.db
desktop.ini

# --- LOGS ---
*.log
npm-debug.log*

# --- PERSONAL TEST FILES ---
# Do not commit your real resume to a public repo.
my-resume.pdf
test-resume.*
/personal/

# --- QA ARTIFACTS ---
# Local screenshot packs and one-off audit tooling — not app code,
# not project documentation, never meant to be shared.
/screenshots/
/scripts/
